<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
		<link rel="stylesheet" href="/stylesheets/style.css" type="text/css" />
        <link href="https://fonts.googleapis.com/css2?family=Anton&display=swap" rel="stylesheet">
		<title>Voter Portal</title>
		<script src="https://code.jquery.com/jquery.js"></script>
		<script src="/js/ballot.js"></script>
        
	</head>
	<body>

   <h1>Ballot</h1>
   <h4 class= "type">{{ type }}</h4>
   {{!-- Added a new H4 called "normalized Input.line1" sourced from the API routes (line 138).  Using as a User ID alternative since we did not source ID to ballot.handlebars" --}}
   <h4 class= "type" hidden id = "address">{{ normalizedInput.line1 }}</h4>

   {{!-- Commented this out because page was reloading after submit button --}}
   {{!-- action="/action_page.php --}}
   <form>
   <h4 class= "type">{{ type }}</h4>
        {{#each contests}}
        <h4 class= "type"></h4>
        <h3 class= "office">{{ office }}</h3>
            {{#each candidates}}
                <input class="radio2" type="radio" data-id="{{this.name}}" name="radio {{../office}}" value="{{name}}">
                <label for="{{../office}}"><h3 class="ballotName"> {{name}}</h3></label><br>
            {{/each}}
        
        {{/each}}
        <br>
        
    </form>
    {{!-- Put outside of the form to stop page reload --}}
    <button id="submit" data-id="submit" > Submit Ballot</button>
    <br>
    <br>
</body>
</html>